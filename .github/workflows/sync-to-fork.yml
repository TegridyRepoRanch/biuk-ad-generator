# Disabled — sync is handled by pushing to both remotes directly.
# The fork (HighestTegridyRepos) and upstream (TegridyRepoRanch) are kept
# in sync via `git push origin main && git push upstream main`.
# Vercel deploys from TegridyRepoRanch via Git integration.

name: Sync to Fork (disabled)
on:
  workflow_dispatch:  # manual only — no longer auto-triggers

jobs:
  sync:
    if: false  # disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "Sync disabled"
